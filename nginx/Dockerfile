FROM nginx

# TODO: comment this line when encryption is not yet active
# COPY nginx_https.conf /etc/nginx/conf.d/default.conf

# TODO: uncomment this line when encryption is not yet active
COPY nginx_http.conf /etc/nginx/conf.d/default.conf